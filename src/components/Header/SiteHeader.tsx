import { Header, Heading } from './SiteHeader.style';

const SiteHeader = () => {
    return (
        <Header>
            <Heading>Number Multiples</Heading>
            <p>Select a number to see the multiples of that&nbsp;number.</p>
        </Header>
    );
};

export default SiteHeader;
